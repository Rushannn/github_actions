name: Third Workflow
on: push

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: List File and Copy Repo
        run: ls -a
      - uses: actions/checkout@v4
      - name: List File
        run: ls -a
